# Start Etcd

Make sure you have Docker configured on your Linux or Mac OS X host.

```bash
./init-etcd.sh start
```

# Compile example

```bash
go build
```

# Run example

```bash
./example -node <IP> -port 5001
```
